#pragma once
#include <iostream>
using namespace std;

class CommentHandler {
public:	
	static void SkipComments(istream inputstream)
	{		
		SkipComments(inputstream);
		SkipComments(outputstream);
	}
private:
	static SkipComments(iostream& datastream) {
		datastream.tell
		while(datastream.showpos)
	}

private:
	static string m_linecomment_tokens[]{ "//", "\\\\" };
	static string m_blockcomment_tokens[][]{ {"/*", "\*"} };
};